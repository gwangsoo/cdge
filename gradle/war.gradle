apply plugin: "war"

bootWar {
    mainClass = "com.hae.ecs.domain.charging.EcsApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
